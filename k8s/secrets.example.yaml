apiVersion: v1
kind: Secret
metadata:
  name: hovesa-secrets
  namespace: hovesa
  labels:
    app.kubernetes.io/name: hovesa
    app.kubernetes.io/part-of: hovesa
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: change-me
  MYSQL_PASSWORD: change-me
  REDIS_PASSWORD: ""
  # Optional overrides for the app stack
  # MYSQL_DATABASE: hovesa
  # MYSQL_USER: hovesa
